fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
aiosqlite==0.20.0

# --- LLM provider: Anthropic Claude ---
anthropic==0.116.0     # httpx pin no longer needed — modern anthropic works with httpx>=0.28
instructor==1.15.4     # structured-output validation of the model's reply (Translation schema)

# --- prompt management (YAML store + Jinja2 templating) ---
jinja2==3.1.6
pyyaml==6.0.3
